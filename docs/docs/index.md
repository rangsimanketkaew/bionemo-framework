# BioNeMo Recipes

BioNeMo Recipes contains runnable model implementations, training recipes, inference workflows, and interpretability examples for biological foundation models.

Use the root `models/`, `recipes/`, and `interpretability/` directories as the source of truth. The generated docs mirror those README files and examples.
